oraclesql-pygments-lexer
========================

Pygments lexer for Oracle SQL and PL/SQL

Adding to .dockerfile syntax should be like
```yaml
RUN /opt/venv/bin/pip install git+https://github.com/feagor/oraclesql-pygments-lexer.git
